#!/usr/bin/env ruby
require File.expand_path("boot")
require 'pry'
MyMigrate.actions
command_set = Pry::CommandSet.new do
  command "help" do
    MyMigrate.help
  end
end
# TODO get rid of pry line trace at start
puts "Type MyMigrate.help for more info"
Pry.start binding, :quiet => true

